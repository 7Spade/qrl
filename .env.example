# MEXC API Configuration
# Get your API credentials from: https://www.mexc.com/user/openapi
# MEXC provides two credentials:
#   - Access Key (also called API Key)
#   - Secret Key (also called API Secret)
MEXC_API_KEY=your_access_key_here
MEXC_API_SECRET=your_secret_key_here

# Optional: Subaccount name (leave empty for main account)
# MEXC_SUBACCOUNT=

# Trading Configuration
SYMBOL=QRL/USDT
BASE_ORDER_USDT=1.0
MAX_POSITION_USDT=1.0
PRICE_OFFSET=0.98

# Redis Cache Configuration (REQUIRED)
# Redis caching is MANDATORY for trading bot operation
# Format: redis://[user:password@]host:port[/db]
# Example: redis://default:123123@redis-18847.c334.asia-southeast2-1.gce.cloud.redislabs.com:18847
REDIS_URL=redis://default:password@your-redis-host:6379

# Cache TTL Configuration (in seconds)
# Default TTL for general cache entries
REDIS_CACHE_TTL=60

# TTL for specific data types (optimized for trading)
# Ticker data changes frequently (fast-moving price data)
REDIS_CACHE_TTL_TICKER=5

# OHLCV candle data (historical data, closed candles rarely change)
# Default: 86400 seconds (24 hours) - suitable for historical analysis
REDIS_CACHE_TTL_OHLCV=86400

# Deals/trades data (moderately changing)
REDIS_CACHE_TTL_DEALS=10

# Order book data (fast-changing depth)
REDIS_CACHE_TTL_ORDERBOOK=5

# Balance data (moderately changing)
REDIS_CACHE_TTL_BALANCE=10

# Redis namespace for environment separation (dev/staging/prod)
REDIS_NAMESPACE=qrl
