# QRL 交易機器人 - 快速開始

## 📋 前置需求

- Python 3.9+
- MEXC 交易所 API 金鑰
- 基本的加密貨幣交易知識

## 🚀 本地開發

### 1. 安裝

```bash
git clone https://github.com/7Spade/qrl.git
cd qrl
pip install -r requirements.txt
```

### 2. 設定

```bash
cp .env.example .env
# 編輯 .env 填入 MEXC API 金鑰
```

### 3. 執行

```bash
# 執行交易機器人
python main.py

# 啟動監控面板
uvicorn web.app:app --reload
```

## ☁️ Google Cloud Run 部署

```bash
# 使用 Cloud Build 部署
gcloud builds submit --config cloudbuild.yaml
```

## 📊 交易策略

**買入條件**（需同時滿足）：
1. 價格 ≤ EMA60 × 1.02（接近支撐位）
2. EMA20 ≥ EMA60（正向動能）

**風險控制**：
- 單筆訂單：50 USDT
- 最大倉位：500 USDT
- 限價折讓：2%

## ⚙️ 參數設定

修改 `config.py`：

| 參數 | 預設值 | 說明 |
|------|--------|------|
| `SYMBOL` | `QRL/USDT` | 交易對 |
| `BASE_ORDER_USDT` | `50` | 單筆金額 |
| `MAX_POSITION_USDT` | `500` | 最大倉位 |
| `PRICE_OFFSET` | `0.98` | 限價折扣 |

## 🔄 自動執行

**Cron（Linux/Mac）**：
```bash
0 9 * * * cd /path/to/qrl && python3 main.py >> logs/bot.log 2>&1
```

**Cloud Scheduler（Cloud Run）**：
```bash
gcloud scheduler jobs create http qrl-trader \
  --schedule="0 9 * * *" \
  --uri="YOUR_CLOUD_RUN_URL/trade" \
  --http-method=POST
```

## 🐛 常見問題

**模組錯誤**：`pip install -r requirements.txt`

**資料庫錯誤**：`rm -rf data/ && python main.py`

**API 錯誤**：檢查 `.env` 中的 API 金鑰

## ⚠️ 風險提示

**加密貨幣交易具有高風險！**本機器人僅供學習使用，不保證獲利。只使用可承受損失的資金。

---

**免責聲明**：僅供教育用途，使用風險自負。
